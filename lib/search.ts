/**
 * API pública de helpers de búsqueda territorial (filtro por región en listados globales y Algolia).
 */
export {
  normRegionSlugForMatch,
  recordMatchesRegionSlug,
} from "@/lib/search/regionTerritoryMatch";
